// Declare interfaces outside the component

// Interface for Name
interface Name {
    title: string;
    first: string;
    last: string;
  }
  
  // Interface for Location
  interface Location {
    city: string;
    state: string;
    country: string;
  }
  
  // Main User Interface
  
  